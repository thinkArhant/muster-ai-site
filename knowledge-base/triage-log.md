# Triage Log
<!-- Per-project audit log of observation dispositions. PM-owned. -->
<!-- Tier-2: read on demand only (during planning, or when auditing past calls) — NEVER a startup read. -->
<!-- Logs ALL dispositions (ACT / DEFER / IGNORE / ESCALATE), chronological. The silent autonomous handles (ACT/DEFER/IGNORE — made without a live founder) are exactly what this log exists to make auditable. -->
<!-- Archive at sprint closeout: move entries from before the current sprint to triage-log-archive.md (mirrors the decision-log.md rule, CLAUDE.md Rule 14). -->
<!-- Disposition flow + routing: team/pm/skills/generic/observation-triage.md. -->

<!-- Format (one line per disposition):
- YYYY-MM-DD | OBS-NNN (HO-NNN) | DISPOSITION | summary | rationale-or-target
  DISPOSITION ∈ ACT | DEFER | IGNORE | ESCALATE
  rationale-or-target: ACT → REQ-NNN / agent; DEFER → pre-launch-checklist or roadmap; IGNORE → why; ESCALATE → Founder Decisions title
-->

## Dispositions

- 2026-07-25 | OBS-001 (HO-001) | ACT | Model plan disagreed across two files — `current-sprint.md:55` named `claude-opus-4-8` while DEC-004 and every queue `Model:` line named `claude-opus-5` | PM error, fixed same session; see DEC-014. Root cause: the switch was applied to the queue with a targeted sed and verified with a grep on that same file, so the sprint board's prose was never checked. Cascade Lag Prevention Protocol requires a repo-wide grep for the old value — that step was skipped.
- 2026-07-25 | OBS-002 (HO-001) | ACT | Roster size and roles that ran differ — L1 says "8 roles standing by"; seven roles actually executed the wave | Confirmed at source by corpus v1.1 and now binding via DEC-013. Encoded in `copy-rules.md` as a named rule under the scope table rather than left as an inventory footnote, because the failure it prevents is a copy failure: narration implying all eight roles worked the wave. Developer's own suggested action was "none needed" — upgraded to ACT since Content, not Developer, is the party at risk.
- 2026-07-25 | HO-001 F1 | ESCALATE→resolved | Session table says 8 sessions; DEC-023 says "7 agent steps" | Escalated to Founder Decisions as non-blocking; answered at source in corpus v1.1 rather than by ruling. Both true: 8 sessions = 7 agent work-steps + the PM review session. See DEC-013.
- 2026-07-25 | OBS-003 (HO-003) → F1 (HO-004) | ESCALATE | A `64ch` column does not measure 64 rendered characters — the built column renders ~90 | Diagnosis accepted, resolution escalated non-halting to Founder Decisions ("Did '~64ch' mean the CSS unit or the measure?"). The Developer filed it as "none needed — inside the spec's ~64ch", which was true of the token and false of the reading; QA restated it correctly at ~90 and disputed the ~66.7 figure as a count of `0` glyphs. It is a founder-authored value with two honest readings, so PM ruled the diagnosis and not the fix (matrix: confidence <7/10 on resolution). Backstopped hard in `pre-launch-checklist.md`. Gates nothing before §3 — §2's narration rail is ~36ch and never enters `--read-max`. DEC-021.3.
- 2026-07-25 | OBS-001 (HO-003) → F2 (HO-004) | ACT | `.instrument` holds a flat 48px inset at every width; a 320px phone spends 96px of a 272px card on padding, leaving a 174px column | Ruled by PM and routed inline to the §2 build step, not as a new step. Both the Developer and QA withheld the fix on the reasoning that `section-02-replay.md` §7.1 budgets §2's mobile core off shell tokens to the tenth of a pixel — that claim is false and neither party checked it against the table: §7.1's rows budget 12+12 terminal and 24px card insets of its own, and at 48px its core would be 568.4px against a 553px viewport. The instinct to protect a signed-off budget was sound; the specific constraint was not, and it travelled through two handoffs into a queue step. Below `--bp-wide` the inset caps at `--gap-flow`, no second breakpoint. DEC-021.1.
- 2026-07-25 | OBS-002 (HO-003) → F3 (HO-004) | ACT | `page-shell.md` §11 says six `<h2>` while §12 draws five, and §11's landmark sentence claims a stencil tag the hero does not have | Applied directly by PM to §11 rather than routed — a correction containing no design call, disclosed in the HO-004 verdict (DEC-019 precedent). §12 and the build were both right.
- 2026-07-25 | OBS-004 (HO-004) | DEFER | Count-up is fixture-verified, not page-verified; a mid-roll value has no `aria-live` guard | The shell instantiates no `[data-countup]` element, so there is nothing on the page to verify against yet and the fixture evidence is real. Deferred to §1/§5 in Sprint 2, carried in `pre-launch-checklist.md` as a soft item so the `aria-live` posture is decided when a real cell ships rather than discovered at launch. QA's own suggested action was "none now" and PM agrees.
- 2026-07-25 | HO-004 audit threshold | ACT | `qa-independent-audit.mjs`'s 45-character floor is arithmetically unsatisfiable at 320px | Replaced, not loosened, and challenged explicitly per `decision-making.md`'s tolerance-loosening rule: 45 × 7.615px = 342.7px is wider than the 320px viewport itself, so no padding value can pass it; the ceiling is ~36 characters at 320px with a zero-inset card. Not a goalpost move — the build is fixed in the same wave, and the replacement (a deterministic ≤20% inset-share assertion plus a reported measure) can still fail. Routed to QA's §2 step with the arithmetic, so QA re-bases rather than deletes. DEC-021.2.
- 2026-07-27 | OBS-001 (HO-018) | ACT | `agent-context/content.md` still carried SP3 as an open 320px overflow at 24 words with a 139-of-163 timed total | Applied by PM at this review — the file said the defect was open in three places after it closed, and Content reads that file at bind time, so a stale "open exactly once, now" would have re-opened a settled item. Refreshed to 19 words, 5 rendered lines against the 6-line card, and 134 of 163, with the growth guardrail preserved.
- 2026-07-27 | OBS-002 (HO-018) | ACT | `qa-independent-audit.mjs:1296` still labels the 320px SP3 row "DEC-027.1 — deferred"; `:1177` prints prose computed live and is still true | Routed to the QA sweep, which owns the audit and is already scheduled — no new step. Report labels, not assertions, so nothing is mis-verified in the meantime; left visible rather than silently corrected by PM because the audit is QA's instrument.
- 2026-07-27 | OBS-003 (HO-018) | IGNORE | The §2 totals-strip value line renders at 2 lines / 246.34px of 272px at 320px, where DEC-027.2 expected a third line | Nothing to act on — the old figure was a prediction, not a guarantee, and no assertion binds on it. Logged so the next §2 measurement does not treat the superseded figure as its baseline.
- 2026-07-27 | OBS-004 (HO-020) | IGNORE | `section-01-copy.md` §2 justifies the ≤9-word headline budget with "at most two lines at 360px"; measured, B and D set three whole-phrase lines at the amended floor | The budget is right and no string changes, so this is a rationale sentence that is off by measurement. Not worth reopening a copy file for; Content's to tidy if §1's copy is opened again for the headline selection, which it will be when the founder picks one at Gate A.
- 2026-07-27 | OBS-005 (HO-024) | ACT | The WebKit harness can measure rendered *geometry* by colour-clustering a QuickLook PNG, not only ink coverage — several specs state cross-engine limits as though it cannot | Noted where REQ-007 was settled and it correctly does **not** rescue scroll-snap: a behaviour still cannot be driven, so the manual ruling stands. What it does change is the ceiling for *static* geometry a spec calls cross-engine-critical, and the QA sweep step's cross-engine criterion is amended to say so, so the wider door is used rather than rediscovered.
- 2026-07-27 | OBS-006 (HO-024) | ACT | `muster-requests-lint.sh` red at 872 active lines against a 300 budget | Resolved by this review, which is the sweep the batched-gate design always deferred it to — Active is now 6 lines and the lint is green. The Developer and UI/UX both flagged it as expected-not-defect and both were right; no framework change is warranted from one sprint shape, but a batched-gate sprint holding six handoffs open by construction is worth carrying to the retrospective as a lint-assumption note rather than fixed locally.

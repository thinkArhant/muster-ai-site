# Architecture Audit Notes (Transient)

<!--
This file is transient. Developer writes it in bootstrap mode during
existing-project onboarding (Phase 5 of reverse-discovery.md,
codebase-audit.md). Contents feed the durable knowledge-base/architecture.md
after founder review + per-item resolution at Phase 6. The entire file is
archived to .muster-archive/onboarding-<date>/ at T+140 cleanup.

Do not edit this file directly in steady-state use — it exists only during
onboarding.

Structure below is illustrative. Developer adapts section headers to the
project's code areas (platforms, services, packages — whatever the natural
units are). Tag semantics (`[verified]` / `[inferred]`) and section
structure are stable across all stacks.
-->

## <Code area — e.g., Web, Backend, iOS, Android, CLI, Service X>

| Claim | Tag | Evidence | Founder action |
|-------|-----|----------|----------------|
| <claim> | [verified] | <file path or concrete observation> | |
| <claim> | [inferred] | <pattern / directory / naming convention> | |

<!-- Add more sections (## <Code area>) as needed for multi-surface projects. -->

## Founder claims verified against code

<!-- If the audit brief included founder claims to verify, report each here
     with result (confirmation / contradiction / no-evidence) + cited evidence.
     If brief had no claims (founder skipped brain-dump), write "(no founder
     claims provided in brief)". -->

- Claim: "<quoted founder claim>" → result: `confirmation` | `contradiction` | `no-evidence`. Evidence: <what was found or not found>

## Skipped

<!-- Required section. List what was NOT read and why.
     Adapt to the stack — examples below cover multiple platforms. -->

- <dir>: <reason — e.g., "vendored dependencies, ~340MB", "build artifacts",
  "DSL not audited in this pass", "out of scope per brief"

## Assumptions the audit made

<!-- When structure is ambiguous, record what the audit assumed and why.
     If no assumptions, write "None flagged this run." -->

- <assumption + reasoning>

## Product signals (for ground-truthing vs questionnaire)

<!-- Signals the audit surfaced from code that PM uses to cross-check
     founder's stated intent in Phase 8. If brief had no product-signal
     requirement, this section can be brief. -->

- Route names: <list>
- User table fields (if obvious): <list>
- External API integrations: <list>
- Visible copy strings (from entry points): <list>
